name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Instala uv
        uses: astral-sh/setup-uv@v4
        with:
          python-version: "3.10"

      - name: Instala dependencias (app + dev)
        run: uv pip install --system -r app/requirements-dev.txt

      - name: ruff
        run: ruff check .

      - name: pytest
        run: pytest
